proc main()


local a := (((5+4)*(2+7))+(7*3))

do while a
    // ... some stuff with a
    // other code
enddo

if .T.
    // true code
else
    // false code
endif

for a:=1 to 10
    if .F.
        exit
    endif
    if .T.
        loop
    endif
    // other code
next
