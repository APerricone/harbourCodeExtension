{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "harbour-dbg",
			"request": "launch",
			"name": "Launch currentFile",
			"program": "${workspaceRoot}/${fileBasenameNoExtension}",
			"debugServer": 4711,
			"workspaceRoot": "${workspaceRoot}"
		}
	]
}