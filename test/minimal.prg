REQUEST HB_CODEPAGE_ITWIN

proc test(n)
   LOCAL c := {1,2,3,{4,5,6}}
   local a := 100, cPrice:="€ 199,99"
   hb_cdpSelect("ITWIN")
   subProc()

   /*switch Mod(a,2)
      case 0
         a/=2
         exit
      case 1
         a*=2
         exit
   endswitch*/
   if a=1

   endif

return

static proc subProc()
   LOCAL c :={1=>"perry",2=>"tony"}
   local a := 50

   ? "ciao"
// Documenti altro doc
// codice sparso

#define I 1